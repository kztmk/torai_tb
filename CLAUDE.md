# Project Guidelines for Claude

## Deployment

Deployments are handled by the user. Do **not** run `firebase deploy` or any deploy commands. After making and building code changes, stop and inform the user what was changed — the user will deploy manually.
